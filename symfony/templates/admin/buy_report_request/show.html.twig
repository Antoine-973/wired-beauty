{% extends 'admin.base.html.twig' %}

{% block title %}{{ 'Buy report request'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'Buy report request'|trans }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ buy_report_request.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_buy_report_request_index') }}">back to list</a>

    {{ include('buy_report_request/_delete_form.html.twig') }}
{% endblock %}
