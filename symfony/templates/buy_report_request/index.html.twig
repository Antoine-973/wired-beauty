{% extends 'base.html.twig' %}

{% block title %}BuyReportRequest index{% endblock %}

{% block body %}
    <h1>BuyReportRequest index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for buy_report_request in buy_report_requests %}
            <tr>
                <td>{{ buy_report_request.id }}</td>
                <td>
                    <a href="{{ path('app_buy_report_request_show', {'id': buy_report_request.id}) }}">show</a>
                    <a href="{{ path('app_buy_report_request_edit', {'id': buy_report_request.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_buy_report_request_new') }}">Create new</a>
{% endblock %}
