{{ form_start(form) }}
    {{ form_widget(form.content, {'attr': {'class': 'article-editor'}}) }}
    <button class="btn wb-btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
