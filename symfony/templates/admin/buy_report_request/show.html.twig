{% extends 'base.html.twig' %}

{% block title %}BuyReportRequest{% endblock %}

{% block body %}
    <h1>BuyReportRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ buy_report_request.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_buy_report_request_index') }}">back to list</a>

    <a href="{{ path('app_admin_buy_report_request_edit', {'id': buy_report_request.id}) }}">edit</a>

    {{ include('buy_report_request/_delete_form.html.twig') }}
{% endblock %}
