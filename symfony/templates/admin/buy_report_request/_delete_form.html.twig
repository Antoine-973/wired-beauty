<form method="post" action="{{ path('app_buy_report_request_delete', {'id': buy_report_request.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ buy_report_request.id) }}">
    <button class="btn">Delete</button>
</form>
