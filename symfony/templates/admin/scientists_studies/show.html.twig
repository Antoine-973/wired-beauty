{% extends 'base.html.twig' %}

{% block title %}Study{% endblock %}

{% block body %}
    <h1>Study</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Title</th>
                <td>{{ study.title }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ study.path??'Unknown'}}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_scientists_studies_index') }}">back to list</a>

    <a href="{{ path('app_scientists_studies_edit', {'id': study.id}) }}">edit</a>

    {{ include('admin/scientists_studies/_delete_form.html.twig') }}
{% endblock %}
